# Middleware Umgebungsvariablen — Vorlage
# Kopieren nach .env (ohne .example) und mit echten Werten befüllen.
# .env selbst NIEMALS committen (siehe .gitignore).

MIDOCEAN_API_KEY=
COTTON_CLASSICS_API_KEY=
MAKITO_CUSTOMER_TOKEN=
